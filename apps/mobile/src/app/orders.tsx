import { ActivityIndicator, FlatList, Pressable, StyleSheet, Text, View } from 'react-native';
import { useEffect, useState } from 'react'; import { router } from 'expo-router'; import { api } from '@/lib/api'; import { useSession } from '@/lib/session';
const GREEN='#0B8F55'; export default function Orders(){const{user}=useSession();const[data,setData]=useState<any[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{if(user)api<any[]>('/orders/mine').then(setData).catch(()=>setData([])).finally(()=>setLoading(false));else setLoading(false)},[user]);if(!user)return <NeedLogin/>;return <View style={styles.screen}><Text style={styles.h1}>Orders</Text>{loading?<ActivityIndicator color={GREEN}/>:<FlatList data={data} keyExtractor={x=>x.id} contentContainerStyle={styles.list} ListEmptyComponent={<Text style={styles.empty}>No orders yet.</Text>} renderItem={({item})=><Pressable style={styles.card}><Text style={styles.title}>{item.ad?.title||'Marketplace order'}</Text><Text style={styles.price}>UGX {Number(item.total).toLocaleString()}</Text><Text style={styles.meta}>{item.status} · Payment {item.paymentStatus}</Text></Pressable>}/>}</View>};function NeedLogin(){return <View style={styles.center}><Text style={styles.h1}>Sign in required</Text><Pressable onPress={()=>router.push('/login')} style={styles.button}><Text style={styles.buttonText}>Log in</Text></Pressable></View>}const styles=StyleSheet.create({screen:{flex:1,backgroundColor:'#F6F8F7',padding:18,paddingTop:30},h1:{fontSize:26,fontWeight:'900',color:'#17202A',marginBottom:14},list:{gap:10,paddingBottom:90},card:{backgroundColor:'#FFF',padding:16,borderRadius:14,borderWidth:1,borderColor:'#E2E8E5'},title:{fontWeight:'800',color:'#17202A'},price:{fontWeight:'900',color:GREEN,marginTop:7},meta:{fontSize:12,color:'#74808B',marginTop:4},empty:{textAlign:'center',padding:35,color:'#74808B'},center:{flex:1,alignItems:'center',justifyContent:'center',backgroundColor:'#F6F8F7'},button:{backgroundColor:GREEN,padding:14,borderRadius:12},buttonText:{color:'#FFF',fontWeight:'900'}});
