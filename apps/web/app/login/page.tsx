'use client'; import {useState} from 'react'; import {api} from '../lib'; import Header from '../components/Header';
export default function Login(){const[email,setEmail]=useState('buyer@minifygadgets.com'),[password,setPassword]=useState('ChangeMe123!'),[msg,setMsg]=useState('');async function go(e:any){e.preventDefault();try{const r=await api('/auth/login',{method:'POST',body:JSON.stringify({email,password})});localStorage.setItem('minify_token',r.accessToken||r.token);localStorage.setItem('minify_user',JSON.stringify(r.user));location.href='/dashboard'}catch(e:any){setMsg(e.message)}}return <><Header/><main className="container section"><div className="panel" style={{maxWidth:480,margin:'auto'}}><h1>Sign in</h1><form className="form" onSubmit={go}><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="Password"/><button className="btn primary">Sign in</button>{msg&&<p>{msg}</p>}</form><p className="muted">Demo buyer: buyer@minifygadgets.com / ChangeMe123!</p><p className="muted auth-switch">New to MINIFY MARKET? <a href="/register">Create an account</a></p></div></main></>}
