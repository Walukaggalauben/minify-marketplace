export const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:4000/api';
export const API_ORIGIN=API.replace(/\/api\/?$/,'');
export const mediaUrl=(url:string)=>url?.startsWith('http')?url:url?.startsWith('/')?`${API_ORIGIN}${url}`:url;
export function getToken(){if(typeof window==='undefined')return null;return localStorage.getItem('minify_token');}
export function clearSession(){if(typeof window==='undefined')return;localStorage.removeItem('minify_token');localStorage.removeItem('minify_user');}
export async function api(path:string,init:RequestInit={}){const token=getToken();const headers=new Headers(init.headers||{});if(!headers.has('Content-Type')&&!(init.body instanceof FormData))headers.set('Content-Type','application/json');if(token)headers.set('Authorization',`Bearer ${token}`);const r=await fetch(`${API}${path}`,{...init,headers,cache:'no-store'});if(r.status===401){clearSession();if(typeof window!=='undefined'&&!location.pathname.startsWith('/login'))location.href='/login?next='+encodeURIComponent(location.pathname+location.search);}if(!r.ok){let message=`Request failed (${r.status})`;try{const d=await r.json();message=Array.isArray(d.message)?d.message.join(', '):(d.message||d.error||message)}catch{try{const t=await r.text();if(t)message=t}catch{}}throw new Error(message)}if(r.status===204)return null;return r.json()}
export const money=(n:number|string)=>`UGX ${Number(n).toLocaleString()}`;
